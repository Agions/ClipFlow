[package]
name = "fablr-domain"
version = "2.2.0"
edition = "2021"
description = "Fablr 核心领域实体模型与数据契约"
authors = ["Agions <agions@qq.com>"]
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
